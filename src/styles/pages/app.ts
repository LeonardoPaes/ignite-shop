
import { styled } from '@/styles';
export const Container = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    // justifyContent: 'center',
    maxHeight: '100vh',
    height: '100vh',
    position: 'relative',
    overflow: 'hidden',
})
